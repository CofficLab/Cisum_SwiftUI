# 构建和修复

逐步修复和构建错误：

1. 构建

2. 解析错误输出：
   - 按文件分组
   - 按严重程度排序

3. 针对每个错误：
   - 显示错误上下文（前后各 5 行）
   - 解释问题
   - 提出修复方案
   - 应用修复
   - 重新运行构建
   - 验证错误已解决

4. 停止条件：
   - 修复引入了新错误
   - 同一错误在 3 次尝试后仍然存在
   - 用户请求暂停

5. 显示摘要：
   - 已修复的错误
   - 剩余的错误
   - 引入的新错误

注意：

- 为了安全起见，一次修复一个错误！
- 使用中文和用户交流
